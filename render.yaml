services:
  - type: web
    name: nailsnice-web
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --noinput
    startCommand: python manage.py migrate --noinput && uvicorn nailsnice.asgi:application --host 0.0.0.0 --port $PORT
